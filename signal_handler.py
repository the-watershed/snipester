import logging

def route_text_to_window(text, target_window):
    """Routes the given text to the specified window and logs the action."""
    logging.info(f"Routing text to {target_window}: {text}")
    # Logic to route text to the appropriate window
    # This could involve updating a GUI element or logging to a file
    print(f"Routing to {target_window}: {text}")  # Example output to console
